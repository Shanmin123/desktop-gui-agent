# 单元测试报告

对应大纲第 2 周交付物。2026-08-30。

运行 `pytest tests/ -q`，共 **48 项**，通过 48 项，失败 0 项。

| 模块 | 覆盖范围 | 用例数 |
|---|---|---|
| `tests/test_schema.py` | 数据格式：屏幕识别结果、动作、执行记录 | 10 |
| `tests/test_perception.py` | 桌面感知：缩放系数、坐标归一化、边界框绘制、图片读写 | 9 |
| `tests/test_control.py` | 桌面控制：10 个动作、坐标换算、安全拦截、批量执行 | 20 |
| `tests/test_models.py` | 大模型调用接口：输出解析、提示词模板 | 9 |

---

## 用例明细

### tests/test_schema.py（10 项）

- test_归一化与像素互转且越界被拒 — 通过
- test_换分辨率后同一归一化坐标落在等比位置 — 通过
- test_元素中心点 — 通过
- test_十个动作类型都能构造 — 通过
- test_构造时即校验 — 通过
- test_终止动作判定 — 通过
- test_动作字典往返且不输出空字段 — 通过
- test_from_dict_容忍json的列表坐标与多余字段 — 通过
- test_轨迹统计与json输出 — 通过
- test_失败步骤记录错误信息 — 通过

### tests/test_perception.py（9 项）

- test_本机4k屏的缩放系数 — 通过
- test_缩放后不超过上限且小屏不放大 — 通过
- test_上限可调且非法分辨率报错 — 通过
- test_resize输出尺寸正确且小图原样返回 — 通过
- test_四角点转归一化外接矩形并裁到边界 — 通过
- test_同一元素在不同分辨率下归一化结果相同 — 通过
- test_画框不改原图且无元素时原样返回 — 通过
- test_imwrite_imread_中文路径往返 — 通过
- test_imread_文件不存在时报错 — 通过

### tests/test_control.py（20 项）

- test_归一化坐标换算到像素 — 通过
- test_控制端分辨率与截图端不同也能点对 — 通过
- test_三种点击 — 通过
- test_拖拽 — 通过
- test_四个方向的滚动[up] — 通过
- test_四个方向的滚动[down] — 通过
- test_四个方向的滚动[left] — 通过
- test_四个方向的滚动[right] — 通过
- test_英文走键盘中文走剪贴板 — 通过
- test_组合键 — 通过
- test_等待与终止动作不产生桌面操作 — 通过
- test_组合键解析与别名归一 — 通过
- test_锁屏类组合键被拒 — 通过
- test_关闭应用的alt_f4放行 — 通过
- test_破坏性命令被拒而正常输入放行 — 通过
- test_dry_run_对显式传入的后端也生效 — 通过
- test_后端抛异常时返回失败而不是崩掉 — 通过
- test_run_遇终止或失败即停 — 通过
- test_每一步都记进history — 通过
- test_移动并读回位置 — 通过

### tests/test_models.py（9 项）

- test_解析json形式 — 通过
- test_解析带前后废话的json — 通过
- test_解析qwen的box标记形式 — 通过
- test_解析裸列表 — 通过
- test_解析失败返回None — 通过
- test_json优先于裸列表 — 通过
- test_中心点 — 通过
- test_提示词填入指令 — 通过
- test_提示词的花括号没被format吃掉 — 通过
