# 单元测试报告

对应大纲第 2 周交付物。2026-08-30。

运行 `pytest tests/ -q`，共 **92 项**，通过 92 项，失败 0 项。

| 模块 | 覆盖范围 | 用例数 |
|---|---|---|
| `tests/test_schema.py` | 数据格式：屏幕识别结果、动作、执行记录 | 16 |
| `tests/test_perception.py` | 桌面感知：缩放系数、坐标归一化、边界框绘制、图片读写 | 21 |
| `tests/test_control.py` | 桌面控制：10 个动作、坐标换算、安全拦截、批量执行 | 46 |
| `tests/test_models.py` | 大模型调用接口：输出解析、提示词模板 | 9 |

---

## 用例明细

### tests/test_schema.py（16 项）

- test_归一化与像素互转 — 通过
- test_换分辨率后同一归一化坐标落在等比位置 — 通过
- test_越界坐标被拒绝 — 通过
- test_元素中心点 — 通过
- test_十个动作类型都能构造 — 通过
- test_缺少必需字段时报错 — 通过
- test_未知动作类型报错 — 通过
- test_滚动方向非法时报错 — 通过
- test_动作坐标必须归一化 — 通过
- test_终止动作判定 — 通过
- test_动作字典往返 — 通过
- test_to_dict_不输出空字段 — 通过
- test_from_dict_接受列表形式的坐标 — 通过
- test_from_dict_忽略多余字段 — 通过
- test_轨迹统计与json输出 — 通过
- test_失败步骤记录错误信息 — 通过

### tests/test_perception.py（21 项）

- test_本机4k屏的缩放系数 — 通过
- test_小屏不放大 — 通过
- test_取三者最小值 — 通过
- test_缩放后不超过两条上限 — 通过
- test_上限可调 — 通过
- test_非法分辨率报错 — 通过
- test_resize输出尺寸与系数一致 — 通过
- test_小图原样返回 — 通过
- test_四角点转归一化外接矩形 — 通过
- test_倾斜框取外接矩形 — 通过
- test_图像尺寸为零时报错 — 通过
- test_越界坐标被裁到0和1 — 通过
- test_同一元素在不同分辨率下归一化结果相同 — 通过
- test_画框不改变图像尺寸且确实画了东西 — 通过
- test_没有元素时原样返回 — 通过
- test_中文路径存图不能用cv2_imwrite — 通过
- test_imwrite_imread_中文路径往返 — 通过
- test_imwrite_自动建目录 — 通过
- test_imread_文件不存在时报错 — 通过
- test_分散取点覆盖不同网格区域 — 通过
- test_取点数量不超过可用元素 — 通过

### tests/test_control.py（46 项）

- test_归一化坐标换算到像素 — 通过
- test_控制端分辨率与截图端不同也能点对 — 通过
- test_点击 — 通过
- test_双击 — 通过
- test_右键 — 通过
- test_拖拽 — 通过
- test_四个方向的滚动[up] — 通过
- test_四个方向的滚动[down] — 通过
- test_四个方向的滚动[left] — 通过
- test_四个方向的滚动[right] — 通过
- test_英文输入走键盘 — 通过
- test_中文输入走剪贴板 — 通过
- test_混合中英文也走剪贴板 — 通过
- test_组合键 — 通过
- test_等待不产生桌面操作 — 通过
- test_终止动作不产生桌面操作 — 通过
- test_组合键大小写和空格都能解析 — 通过
- test_组合键别名归一 — 通过
- test_空组合键报错 — 通过
- test_锁屏类组合键被拒 — 通过
- test_win_l_被拒 — 通过
- test_关闭应用的alt_f4放行 — 通过
- test_破坏性输入被拒[format c:] — 通过
- test_破坏性输入被拒[rm -rf /] — 通过
- test_破坏性输入被拒[shutdown -s -t 0] — 通过
- test_破坏性输入被拒[del /f /s /q C:\\] — 通过
- test_破坏性输入被拒[diskpart] — 通过
- test_破坏性输入被拒[mkfs.ext4] — 通过
- test_正常输入不被误伤[format the paragraph] — 通过
- test_正常输入不被误伤[format this cell as currency] — 通过
- test_正常输入不被误伤[\u5220\u9664\u8fd9\u4e00\u884c] — 通过
- test_正常输入不被误伤[hello] — 通过
- test_正常输入不被误伤[how to shut down the app] — 通过
- test_正常输入不被误伤[rm is a unix command] — 通过
- test_文本黑名单挡不住上下文相关的危险 — 通过
- test_后端抛异常时返回失败而不是崩掉 — 通过
- test_每一步都记进history — 通过
- test_耗时被记录 — 通过
- test_run_遇终止动作停止 — 通过
- test_run_遇失败停止 — 通过
- test_dry_run_默认用假后端 — 通过
- test_移动记录并更新位置 — 通过
- test_移动到归一化坐标换算出的像素 — 通过
- test_dry_run_对显式传入的后端也生效 — 通过
- test_dry_run_下等待不真的sleep — 通过
- test_dry_run_仍然执行安全拦截 — 通过

### tests/test_models.py（9 项）

- test_解析json形式 — 通过
- test_解析带前后废话的json — 通过
- test_解析qwen的box标记形式 — 通过
- test_解析裸列表 — 通过
- test_解析失败返回None — 通过
- test_json优先于裸列表 — 通过
- test_中心点 — 通过
- test_提示词填入指令 — 通过
- test_提示词的花括号没被format吃掉 — 通过
