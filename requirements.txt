# 基础环境：Python 3.10，torch 2.6+cu124（随 Anaconda 环境安装，不在此声明）

# 版本固定，改动会影响 OCR 与 transformers 的可用性
numpy==1.26.4                       # 2.x 与 easyocr、paddleocr 的编译版本不兼容
opencv-python-headless==4.10.0.84   # 5.x 要求 numpy>=2
huggingface-hub>=1.3.0,<2.0         # transformers 5.3.0 的要求

# 桌面感知
mss
opencv-contrib-python==4.10.0.84
easyocr
paddleocr
paddlepaddle

# 桌面控制
pyautogui
pynput

# 大模型与 Agent
transformers
accelerate
peft
bitsandbytes
datasets
langchain
langchain-community

# 可视化与测试
matplotlib
seaborn
pytest
